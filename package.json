{
  "name": "imessage-coding",
  "version": "0.1.0",
  "private": true,
  "description": "iMessage-driven remote control + steering for Claude Code coding sessions.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bun run --filter '*' lint",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "//overrides": "better-auth@1.6.12's kysely-adapter imports DEFAULT_MIGRATION_TABLE from kysely's entry, which kysely 0.29.x dropped (it survives in 0.28.17, still within the adapter's peer range ^0.28.17 || ^0.29.0). Pin kysely to 0.28.17 so next build resolves a single, compatible copy.",
  "overrides": {
    "kysely": "0.28.17"
  }
}
