{
  "mcpServers": {
    "imsg-device": {
      "command": "bun",
      "args": ["run", "--cwd", "${CLAUDE_PLUGIN_ROOT}", "--silent", "start"],
      "env": {
        "IMSG_AGENT_KIND": "codex"
      }
    }
  }
}
