{
  "name": "@imsg/device",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Productized Claude Code channel plugin: AFK-aware permission relay + chat bridge to the imessage-coding cloud control plane.",
  "bin": {
    "imsg": "./bin/imsg.ts"
  },
  "scripts": {
    "start": "bun run ./src/channel.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@imsg/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
