/**
 * @imsg/shared — the contract every package imports.
 *
 * Barrel: enums (const-objects + derived union types), shared data types, and
 * dependency-light runtime validators.
 */
export * from './enums.ts';
export * from './types.ts';
export * from './validators.ts';
